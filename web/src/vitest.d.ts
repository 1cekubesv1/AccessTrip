// Loads the @testing-library/jest-dom matcher augmentation for Vitest's `expect`
// (toBeInTheDocument, toHaveTextContent, …) so tsc sees them in *.test.tsx files.
import '@testing-library/jest-dom/vitest'
